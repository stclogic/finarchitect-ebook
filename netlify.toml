[[redirects]]
  from = "/landing"
  to = "/landing.html"
  status = 200

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# HTML 파일 캐시 방지 — 코드 수정이 즉시 반영되도록
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
